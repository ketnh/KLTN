{{ form_start(form) }}
{{ form_errors(form) }}  
Welcome {{app.user.username}}
<table>
    <tr>
        <td>ID</td>
        <td>Name</td>
        <td>Price</td>
        <td>Description</td>
        <td>Delete</td>
        <td>Edit</td>
    </tr>
    {% for product in products %}
    <tr>
        <td>{{product.id}}</td>
        <td>{{product.name}}</td>
        <td>{{product.price}}</td>
        <td>{{product.description}}</td>    
        <td>{{form_widget(form.user[product.id])}}</td>
        <td><a href="{{path('acme_show_edit',{'id':product.id})}}">Edit</a></td>
    </tr>    
    {% endfor%}        
</table>
{{form_end(form)}}
<td><a href="{{path('logout')}}">Logout</a></td>